# Compound 复利引擎 🌱

> 每日存入微小行动，看见时间的力量。

Compound 是一个个人成长记录工具，帮助你每天"存入"思考和行动，通过复利公式可视化成长轨迹。内置 AI 认知陪练（Gemini 3 Pro），与你的每日思考进行苏格拉底式对话。

## ✨ 功能

- **📝 每日存入** — 记录今天的思考、打卡健康习惯、设定明日计划
- **📊 成长曲线** — 基于复利公式（1.01^n）可视化你的持续积累
- **🧠 认知陪练** — AI 多轮对话，追问你的思考、连接历史记录、挑战薄弱环节（支持 5 轮深度对话）
- **📅 时间线** — 回顾过去的记录，看到思维的演化
- **☁️ 云端同步** — Supabase 实时备份，跨设备数据不丢失
- **🌐 随处访问** — 部署在 Vercel，手机/电脑/平板均可使用

## 🛠 技术栈

| 层 | 技术 |
|---|------|
| 前端 | Vite + Vanilla JS + Chart.js |
| 样式 | 自定义 CSS（暗色主题、Glassmorphism） |
| 后端 | Supabase（Database + Edge Functions） |
| AI | Gemini 3 Pro（via Edge Function 代理） |
| 部署 | Vercel（前端）+ Supabase（后端） |

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`，填入你的 Supabase 项目信息：

```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

> 没有 Supabase 也能用，数据会保存在 localStorage（纯本地模式）。

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. （可选）配置 AI 认知陪练

1. 在 [Supabase Dashboard](https://supabase.com/dashboard) 创建项目
2. 执行 `supabase/migrations/` 中的 SQL 迁移
3. 在 Edge Function Secrets 中添加 `GEMINI_API_KEY`
4. 部署 Edge Function：`supabase functions deploy analyze`

## 📁 项目结构

```
compound/
├── src/
│   ├── main.js              # 应用入口 + 路由
│   ├── pages/
│   │   ├── dashboard.js      # 仪表盘（统计、曲线、AI 对话）
│   │   ├── checkin.js        # 每日存入页面
│   │   └── history.js        # 时间线页面
│   ├── lib/
│   │   ├── ai.js             # AI 分析 + 多轮对话
│   │   ├── compound.js       # 复利计算引擎
│   │   ├── storage.js        # 本地 + 云端存储
│   │   ├── supabase.js       # Supabase 客户端
│   │   ├── quotes.js         # 每日名言
│   │   └── router.js         # 前端路由
│   └── styles/
│       └── main.css          # 全局样式系统
├── supabase/
│   ├── migrations/           # 数据库迁移
│   └── functions/
│       └── analyze/          # AI Edge Function
└── index.html
```

## 🧠 AI 认知陪练

不是鸡汤教练，是思维搏击伙伴：

- **追问**（苏格拉底式）— 直接钻进你的思考，而不是说"很有深度"
- **连接**（第二大脑）— 发现今天和过去记录之间的隐性关联
- **挑战**（反脆弱测试）— 找到思考链中最薄弱的环节

支持最多 5 轮对话，每轮都基于完整的对话历史继续深入。

## 📄 License

MIT
